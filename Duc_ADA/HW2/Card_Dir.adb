package body Card_Dir is
   
end Card_Dir;
   --  An enumeration type
-- begin
--    for I in Days loop
--       case I is
--          when Saturday .. Sunday =>
--             Put_Line ("Week end!");

--          when Monday .. Friday =>
--             Put_Line ("Hello on " & Days'Image (I));
--             --  'Image attribute, works on enums too
--       end case;
--    end loop;
-- end Enumeration_Example;